# --- 1. PYTHON BASICS ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# --- 2. VIRTUAL ENVIRONMENTS ---
# (Crucial! Never upload your environment)
.venv/
venv/
ENV/
env/
waste_env_wsl/

# --- 3. VS CODE & SYSTEM FILES ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# --- 4. MACHINE LEARNING DATA (Too big for Git) ---
# We will track these with DVC instead
*.zip
*.tar.gz

# --- 5. MODELS & WEIGHTS (Too big for Git) ---
models/*.pt  # Ignore the heavy model files
*.pt
*.pth
*.onnx
runs/

# --- 6. LOGS & METRICS ---
mlruns/
*.log

# --- 7. SECRETS (Never upload keys!) ---
.env
secrets.json